extends base

block title
  title Error | #{error.statusCode} #{error.message}

block content
  main.main
    .error
      .error__title
        h2.heading-secondary.heading-secondary--error #{error.statusCode} Error
        h2.error__emoji 😢 🤯
      .error__msg= error.message