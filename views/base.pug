doctype html
html
   head
      block head 
         meta(charset="UTF-8")
         meta(name="viewport" content="width=device-width, initial-scale=1.0")
         link(rel="preconnect" href="https://fonts.googleapis.com")
         link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
         link(href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,300;0,700;1,300&display=swap" rel="stylesheet")
         link(rel="stylesheet", href="/css/style.css", type="text/css")
         link(rel="shortcut icon", type="image/png", href="/img/favicon.png")
         script(src="https://js.stripe.com/v3/")
      //- * we cna also use block script here but we have other way to do it that create block for head and then add the script in the end of block head in tour page
      //- block script
      block title
         title Natours | #{title}
   body(data-alert=`${alert ? alert : ''}`)
      // Header
      include _header
      
      // CONTENT
      //- * so to extend the first step we need define block
      block content
         h1 This is a placeholder heading
         //- ! keep in mind that we can use data we passed in overview and tour page in this base template
         //- * because we extends base in two these pages files right so the base template still have access to local and it can get all data
         h2= title 
         h2 #{tour}
         //- ! this content here only display if in overview and tour page we do not custom this block content

      // FOOTER
      include _footer
      script(src="https://unpkg.com/axios/dist/axios.min.js")
      script(type='module' src='/js/bundle.js') 
